export class UMorsePath {
  add: string;
  get: string;
  remove: string;
  lastSync: string;
  sync: string;
  constructor(
    add?: string,
    get?: string,
    remove?: string,
    lastSync?: string,
    sync?: string
  );
}
